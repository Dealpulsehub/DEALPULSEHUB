# Dependencies
node_modules/
yarn.lock
# package-lock.json se trackea a propósito: los workflows de CI usan `npm ci`,
# que requiere un lockfile exacto y commiteado para ser reproducible.

# Environment variables (SENSITIVE)
.env
.env.local
.env.*.local
.npmrc

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
*.swn
.DS_Store

# Build outputs
dist/
build/
*.tsbuildinfo

# Storybook
storybook-static/
.storybook/node_modules/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids/
*.pid
*.seed

# OS files
Thumbs.db

# Claude Code / AIOX Internal (NEVER commit)
.ai/
.aiox-core/
.knowledge-ingestion/
.n8n/
.playwright-mcp/

# Claude Code project config — trackeado a propósito desde 2026-08-10:
# agents/, hooks/, rules/, agents.yaml y settings.json son la gobernanza real
# del proyecto (agentes, hooks de enforcement, agent-authority.md) y deben
# sobrevivir a un `git clone` nuevo. Solo lo genuinamente local/personal o
# efímero queda ignorado:
.claude/settings.local.json
.claude/state/

# Google credentials (SENSITIVE - NEVER commit)
.google-service-account.json

# Encryption vault (SENSITIVE - master key + encrypted credentials)
.vault/

# Temp/Generated files (específicos de DealPulseHub)
nul
debug-storybook.log
